// AI functionality has been removed.
// This file is kept as a placeholder to prevent import errors if it is referenced elsewhere, 
// though it appears unused in the current version.

export const askGeminiLatex = async (prompt: string): Promise<string> => {
  console.warn("AI features are disabled.");
  return "AI features are disabled.";
};
